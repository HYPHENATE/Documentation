# Release Notes

Release-note standardisation for this repo is still being decided.

For now, use the package `README.md` and `sfdx-project.json` as the source of truth for version history and latest installable package version.
