# How Custom Flow LWC Components Works

This package acts as a shared component library for Flow-oriented Salesforce solutions.

The general pattern is straightforward: install the package, expose the relevant components to Flow or page builders, and then use those components inside your process-specific flows and Lightning experiences.

The package also includes an example inactive flow that the README explicitly says should not be activated as-is.
