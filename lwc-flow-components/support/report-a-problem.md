# Report a Problem

When reporting a problem, include which specific component is involved, where it is used, and whether the issue occurs inside Flow, on a record page, or in a custom Lightning context.
