# User Guide

Admins and builders usually consume this package by adding the relevant components into Flow screens and related Lightning experiences.

Because the package is a library, usage varies by component and implementation, and teams often adopt it incrementally across multiple flows.
