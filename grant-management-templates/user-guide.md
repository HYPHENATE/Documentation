# User Guide

This package is usually consumed by implementation teams rather than end users directly.

Teams deploy it after the prerequisite packages are already present and then use the packaged assets as starting points for their configured grant-management experience.
