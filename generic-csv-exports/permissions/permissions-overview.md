# Permissions Overview

Generic CSV Exports includes one packaged permission set:

- `CSV_Export_Access`

This permission set is intended to give users access to the operational export record and the package functionality needed to run or monitor CSV jobs.
