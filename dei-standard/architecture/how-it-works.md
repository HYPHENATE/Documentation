# How DEI Standard Works

DEI Standard combines a custom DEI data model with mapping metadata, screen flows, and invocable Apex.

At a high level, the package stores DEI responses in custom objects, uses metadata to align those values to reporting codes, and supports collection or processing through packaged automation.
