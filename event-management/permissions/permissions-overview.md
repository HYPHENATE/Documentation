# Permissions Overview

The package includes an `Events_Administrator` permission set for users who manage event setup and operational records.

This permission set provides access to the Event Management app, the core event objects, and the fields needed to configure events, tickets, roles, registrations, and engagement tracking.
