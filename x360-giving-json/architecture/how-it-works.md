# How 360Giving Export Works

The package loads configured settings and mapping metadata, retrieves the relevant source records, serializes them into the expected structure, and can run the export process in batch mode when needed.
