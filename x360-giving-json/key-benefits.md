# Key Benefits

## Structured JSON Export Pattern

The package provides a configurable way to generate JSON outputs instead of relying on one-off export scripts.

## Metadata-Driven Mapping

Settings, tags, child tags, and organisation ID metadata allow teams to shape exports without rewriting the core export engine.

## Batch And Controller Support

The package includes controller and batch-generation classes that support operational export use.
