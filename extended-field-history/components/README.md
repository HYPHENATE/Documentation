# Components

The package includes an `extendedFieldHistory` Lightning Web Component for rendering audit history to users.

This component is the main end-user entry point for the package and is designed for Lightning record pages where recent field changes should be visible without running a report.
