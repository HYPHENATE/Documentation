# Data Model

Core packaged data structures include:

- `FieldAuditHistory__c`
- `ExtendedFieldAuditHistory__mdt`
- `ExtendedFieldHistoryControl__c`

Together these structures define what should be tracked, store the resulting history, and control whether history rows can later be edited or deleted.
