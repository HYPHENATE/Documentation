# Configuration

Configuration typically includes:

- assigning the packaged audit permission set
- defining tracked fields in `ExtendedFieldAuditHistory__mdt`
- reviewing edit and delete control settings
- placing the packaged history component where users should review audit data
