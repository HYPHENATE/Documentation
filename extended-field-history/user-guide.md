# User Guide

Admins configure the tracking metadata and assign the packaged audit access permission set.

End users and administrators can then review field-change history through the packaged record views and reports where the audit data is exposed.
