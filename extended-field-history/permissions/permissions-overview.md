# Permissions Overview

The package includes an `ExtendedFieldAuditIT` permission set for users who need access to the field-audit functionality.

This permission set grants access to the audit objects, Apex classes, custom metadata, and control settings used by the package.
