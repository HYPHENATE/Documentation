# JSONParser

## API Name
`JSONParser`

## Description

hyphen8 version of https://github.com/open-force/jsonparse utility class to support with the management / handling of JSON

---

## Sharing Model
`with sharing`

## Tested By
`JSONParserTest`

---

## Key Responsibilities

| Responsibility | Detail |
|------|------|
| Primary purpose | hyphen8 version of https://github.com/open-force/jsonparse utility class to support with the management / handling of JSON |
| Utility behaviour | Provides reusable helper behaviour for Apex development and framework support. |
| Callable methods | Exposes 20 callable method(s) for package or implementation use. |

---

## Public/Internal Methods

| Method | Purpose |
|------|------|
| `get` | Returns or loads data used by the class. |
| `getCurrentNodeAsListIndex` | Returns or loads data used by the class. |
| `getWrapperMapParse` | Returns or loads data used by the class. |
| `exists` | Supports internal or reusable behaviour within the class. |
| `asList` | Supports internal or reusable behaviour within the class. |
| `isObject` | Checks whether a condition is true. |
| `isArray` | Checks whether a condition is true. |
| `toStringPretty` | Supports internal or reusable behaviour within the class. |
| `getBlobValue` | Returns or loads data used by the class. |
| `getBooleanValue` | Returns or loads data used by the class. |
| `getDatetimeValue` | Returns or loads data used by the class. |
| `getDateValue` | Returns or loads data used by the class. |
| `getDecimalValue` | Returns or loads data used by the class. |
| `getDoubleValue` | Returns or loads data used by the class. |
| `getIdValue` | Returns or loads data used by the class. |
| `getIntegerValue` | Returns or loads data used by the class. |
| `getLongValue` | Returns or loads data used by the class. |
| `getStringValue` | Returns or loads data used by the class. |
| `getTimeValue` | Returns or loads data used by the class. |
| `getValue` | Returns or loads data used by the class. |

---

## Important Notes

| Topic | Detail |
|------|------|
| Class Type | This Apex file is implemented as a `class`. |
| Package Role | `JSONParser` is part of the ApexTools reusable framework and should be understood in the context of the wider package design. |
| Security Context | This class declares an explicit sharing model and should be used consistently with the package security standards. |
| Documentation Depth | This page is a generated overview and may be expanded later with deeper implementation notes if needed. |

---

## Related Classes

- [DescribeCache](./DescribeCache.md)
- [QueryBuilder](./QueryBuilder.md)
- [SObjectIndex](./SObjectIndex.md)
- [TypeLoader](./TypeLoader.md)
