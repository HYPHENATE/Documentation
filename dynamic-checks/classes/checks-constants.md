# ChecksConstants

`ChecksConstants` stores the key string constants used across the package.

## Purpose

It centralises values such as:

- filter types
- field operators
- record applicability options
- delimiters used for custom answer values

This helps keep the filtering and checklist logic more maintainable.
