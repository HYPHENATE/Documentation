# CheckCategory

`CheckCategory` is a wrapper class used by the Lightning UI.

## Purpose

It groups related checks into a category and carries the category name, description, order, and owner-edit setting needed by the user interface.
