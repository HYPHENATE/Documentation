# Check

`Check` is a lightweight Apex wrapper class used to send UI-friendly data to Lightning components.

## Purpose

It packages a `Check__c` record together with extra display information such as help text, comment settings, owner editability, and custom answer metadata.
